# Anchors pytest's rootdir here so the webapp's test suite stays independent of
# the pipeline repo's pytest configuration (and its top-level `tests` package).
# Run from webapp/: python -m pytest tests/
[pytest]
testpaths = tests
